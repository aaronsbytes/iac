all:
  children:
    server:
      hosts:
        lotus-master-node:
          ansible_host: lotus-master-node
          ansible_user: root

        lotus-stream:
          ansible_host: lotus-stream
          ansible_user: root

        lotus-vault:
          ansible_host: lotus-vault
          ansible_user: root

        lotus-cloud:
          ansible_host: lotus-cloud
          ansible_user: root

        lotus-vps0:
          ansible_host: lotus-vps0
          ansible_user: root

    desktop:
      hosts:
        #void:
        #  ansible_host: "{{ void.host }}"
        #  ansible_user: "root"
        #  ansible_ssh_pass: "{{ void.root_password }}"

        aaron_workstation:
          ansible_host: "{{ aaron_workstation.host }}"
          ansible_user: "root"
          ansible_ssh_pass: "{{ aaron_workstation.root_password }}"
